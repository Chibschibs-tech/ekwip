version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: ekwip-dev-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ekwip_dev
      POSTGRES_PASSWORD: ekwip_dev_password
      POSTGRES_DB: ekwip_dev
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ekwip_dev"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ekwip-network

volumes:
  postgres_data:
    driver: local

networks:
  ekwip-network:
    driver: bridge


